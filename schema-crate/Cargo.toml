[package]
name = "r-typeshed-schema"
description = "Authoring schema for r-typeshed, the semantic catalog for ry."
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "MIT"
repository = "https://github.com/sims1253/r-typeshed"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
